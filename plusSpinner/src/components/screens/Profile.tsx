import React from "react";
import { SafeAreaView, Text } from "react-native";

export default () => {
  return (
    <SafeAreaView
      style={{
        flex: 1,
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <Text>Profile</Text>
    </SafeAreaView>
  );
};
